-- ACS Memory System Setup
-- Version 1.0 - January 2025
-- This creates a unified memory architecture for all agents

\c acs_context_db;

-- Enable required extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- Note: pgvector needs to be installed separately:
-- sudo apt install postgresql-15-pgvector

-- [Previous SQL content remains the same]
-- Truncated for brevity but includes all tables from previous message
